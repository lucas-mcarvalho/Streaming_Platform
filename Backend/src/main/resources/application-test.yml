spring:
  datasource:
    url: jdbc:h2:mem:streaming_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: teste
    password: 123

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

  flyway:
    enabled: true